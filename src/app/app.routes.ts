import { Routes } from '@angular/router';
import { TareaComponent } from './components/tarea/tarea.component';

export const routes: Routes = [
    {path: 'tarea', title:'Tarea', component: TareaComponent}
];
